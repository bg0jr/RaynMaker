<UserControl x:Class="RaynMaker.Import.Web.Views.WebSpyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:prism="http://www.codeplex.com/prism"
             xmlns:pn="http://github.com/ronin4net/plainion"
             xmlns:l="clr-namespace:RaynMaker.Import.Web.Views"
             xmlns:winforms="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="700">
    <!--
    <i:Interaction.Triggers>
        <prism:InteractionRequestTrigger SourceObject="{Binding InvokeRequest, Mode=OneWay}">
            <pn:PopupViewAction WindowWidth="1200" WindowHeight="700"/>
        </prism:InteractionRequestTrigger>
    </i:Interaction.Triggers>
    -->
    <DockPanel LastChildFill="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid DockPanel.Dock="Top" >
            <Menu>
                <Menu.ItemsPanel>
                    <ItemsPanelTemplate>
                        <DockPanel HorizontalAlignment="Stretch"/>
                    </ItemsPanelTemplate>
                </Menu.ItemsPanel>
                <MenuItem Header="_Tools">
                    <MenuItem Header="_Validation ..." Command="{Binding StartValidationCommand}" />
                </MenuItem>
            </Menu>
        </Grid>

        <DockPanel LastChildFill="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="3">
            <Grid DockPanel.Dock="Top" DataContext="{Binding AddressBar}">
                <l:AddressBarView/>
            </Grid>

            <Grid DockPanel.Dock="Bottom">
                <TabControl Height="140">
                    <TabItem Header="Navigation" DataContext="{Binding Navigation}">
                        <l:NavigationView Margin="2"/>
                    </TabItem>
                    <TabItem Header="Data description" >
                        <Grid Margin="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Selected path" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" x:Name="myPath" Margin="5,2,0,0"/>
                            <TextBlock Grid.Row="0" Grid.Column="4" Text="=>" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="0" Grid.Column="5" x:Name="myValue" Margin="5,2,0,0"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Dimension" VerticalAlignment="Center"/>
                            <ComboBox Grid.Row="1" Grid.Column="1" Width="100" x:Name="myDimension"  Margin="5,2,0,0" SelectionChanged="myDimension_SelectedIndexChanged"/>

                            <TextBlock Grid.Row="1" Grid.Column="2" Text="Series name contains" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            <TextBox Grid.Row="1" Grid.Column="3" x:Name="mySeriesName"  Margin="5,2,0,0" TextChanged="mySeriesName_TextChanged"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Row header column" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="2" Grid.Column="1" x:Name="myRowHeader" Margin="5,2,0,0" TextChanged="myRowHeader_TextChanged"/>

                            <TextBlock Grid.Row="2" Grid.Column="2" Text="Skip rows" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            <TextBox Grid.Row="2" Grid.Column="3" Width="50" HorizontalAlignment="Left" x:Name="mySkipRows" Margin="5,2,0,0" TextChanged="mySkipRows_TextChanged"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Column header row" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="3" Grid.Column="1" x:Name="myColumnHeader" Margin="5,2,0,0" VerticalAlignment="Center" TextChanged="myColumnHeader_TextChanged"/>

                            <TextBlock Grid.Row="3" Grid.Column="2" Text="Skip columns" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            <TextBox Grid.Row="3" Grid.Column="3" Width="50" HorizontalAlignment="Left" x:Name="mySkipColumns" Margin="5,2,0,0" VerticalAlignment="Center" TextChanged="mySkipColumns_TextChanged"/>

                            <Button Grid.Row="3" Grid.Column="4" Content="Reset" Margin="3" x:Name="myReset" Click="myReset_Click"/>
                            <Button Grid.Row="3" Grid.Column="5" Content="Search path" Margin="3" x:Name="mySearchPath" Click="mySearchPath_Click"/>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Grid>

            <WindowsFormsHost HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0">
                <winforms:WebBrowser x:Name="myBrowser"/>
            </WindowsFormsHost>
        </DockPanel>
    </DockPanel>
</UserControl>
